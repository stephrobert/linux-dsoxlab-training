# Drill — AppArmor

> Prepare: `dsoxlab provision` then `dsoxlab run drill-apparmor`

**4 tasks, 100 points, 15 minutes. No hints.** LFCS only (RHEL → `drill-selinux`).

```bash
dsoxlab challenge drill-apparmor   # the subject
dsoxlab check drill-apparmor       # score it
```
