# Drill — AppArmor

> Préparer : `dsoxlab provision` puis `dsoxlab run drill-apparmor`

**4 tâches, 100 points, 15 minutes. Aucun indice.** LFCS uniquement
(RHEL → `drill-selinux`).

```bash
dsoxlab challenge drill-apparmor   # le sujet
dsoxlab check drill-apparmor       # se faire noter
```
