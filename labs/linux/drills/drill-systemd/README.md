# Drill — systemd, timers and scheduling

> Prepare: `dsoxlab provision` then `dsoxlab run drill-systemd`

**5 tasks, 100 points, 25 minutes. No hints.** Shared between RHCSA and LFCS.

```bash
dsoxlab challenge drill-systemd        # the subject
dsoxlab check drill-systemd            # score it (AlmaLinux)
dsoxlab check drill-systemd -t ubuntu  # score it (Ubuntu)
```
