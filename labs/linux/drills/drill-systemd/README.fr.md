# Drill — systemd, timers et planification

> Préparer : `dsoxlab provision` puis `dsoxlab run drill-systemd`

**5 tâches, 100 points, 25 minutes. Aucun indice.** Partagé entre RHCSA et LFCS.

```bash
dsoxlab challenge drill-systemd        # le sujet
dsoxlab check drill-systemd            # se faire noter (AlmaLinux)
dsoxlab check drill-systemd -t ubuntu  # se faire noter (Ubuntu)
```
