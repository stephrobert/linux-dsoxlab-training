# Drill — SELinux

> Prepare: `dsoxlab provision` then `dsoxlab run drill-selinux`

**4 tasks, 100 points, 20 minutes. No hints.** RHCSA only (Debian → `drill-apparmor`).
Contexts are checked **after a relabel**.

```bash
dsoxlab challenge drill-selinux   # the subject
dsoxlab check drill-selinux       # score it
```
