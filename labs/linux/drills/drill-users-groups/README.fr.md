# Drill — utilisateurs, groupes et délégation

> Préparer : `dsoxlab provision` puis `dsoxlab run drill-users-groups`

**5 tâches, 100 points, 20 minutes. Aucun indice.** Partagé entre RHCSA et LFCS.

```bash
dsoxlab challenge drill-users-groups        # le sujet
dsoxlab check drill-users-groups            # se faire noter (AlmaLinux)
dsoxlab check drill-users-groups -t ubuntu  # se faire noter (Ubuntu)
```
