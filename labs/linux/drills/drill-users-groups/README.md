# Drill — users, groups and delegation

> Prepare: `dsoxlab provision` then `dsoxlab run drill-users-groups`

**5 tasks, 100 points, 20 minutes. No hints.** Shared between RHCSA and LFCS.

```bash
dsoxlab challenge drill-users-groups        # the subject
dsoxlab check drill-users-groups            # score it (AlmaLinux)
dsoxlab check drill-users-groups -t ubuntu  # score it (Ubuntu)
```
