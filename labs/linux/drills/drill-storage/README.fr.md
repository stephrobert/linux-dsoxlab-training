# Drill — partitions, LVM et swap

> Préparer : `dsoxlab provision` puis `dsoxlab run drill-storage`

**5 tâches, 100 points, 25 minutes. Aucun indice.** Partagé entre RHCSA et LFCS.

```bash
dsoxlab challenge drill-storage        # le sujet
dsoxlab check drill-storage            # se faire noter (AlmaLinux)
dsoxlab check drill-storage -t ubuntu  # se faire noter (Ubuntu)
```
