# Drill — partitions, LVM and swap

> Prepare: `dsoxlab provision` then `dsoxlab run drill-storage`

**5 tasks, 100 points, 25 minutes. No hints.** Shared between RHCSA and LFCS.

```bash
dsoxlab challenge drill-storage        # the subject
dsoxlab check drill-storage            # score it (AlmaLinux)
dsoxlab check drill-storage -t ubuntu  # score it (Ubuntu)
```
