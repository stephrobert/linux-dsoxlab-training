id: l4-network-static-persist
title: "Configure a persistent static IPv4 with NetworkManager"
description: "Create a NetworkManager connection with a static IPv4 (manual method) that survives reboot — proven by the on-disk profile and the live address."
section: l4
bloc_order: 2
lab_type: lab
level: l4
track:
  - reseau
skills:
  - nmcli
  - networkmanager
  - static-ip
  - persistence
difficulty: intermediate
estimated_time: 25m
runtime:
  type: vm
  # On reste sur le poste : c'est à l'apprenant de taper son ssh vers
  # la machine du lab, et dsoxlab n'est installé que sur le poste.
  session: local
  default: rhel
  snapshot_required: false
  targets:
    - name: rhel
      host: alma-rhcsa-1.lab
      label_en: "AlmaLinux 10 (nmcli, NetworkManager)"
      label_fr: "AlmaLinux 10 (nmcli, NetworkManager)"
distros:
  - rhel10
  - almalinux10
doc_url: https://blog.stephane-robert.info/docs/admin-serveurs/linux/reseau/networkmanager/
validation:
  functional: true
  security: false
  persistence_after_reboot: true
certification_tags:
  - rhcsa
