id: l4-bridge-bonding
title: "Aggregate links: an active-backup bond under a bridge with nmcli"
description: "Build an active-backup bond over two slave interfaces and put a bridge on top, persistently with NetworkManager — proven by the bonding state and the bridge port."
section: linux
type: lab
level: l4
track:
  - reseau
skills:
  - nmcli
  - bonding
  - bridge
  - networkmanager
  - link-aggregation
difficulty: advanced
estimated_time: 30m
runtime:
  type: vm
  # On reste sur le poste : c'est à l'apprenant de taper son ssh vers
  # la machine du lab, et dsoxlab n'est installé que sur le poste.
  session: local
  default: rhel
  snapshot_required: false
  targets:
    - name: rhel
      host: alma-rhcsa-1.lab
      label_en: "AlmaLinux 10 (nmcli bond + bridge)"
      label_fr: "AlmaLinux 10 (nmcli bond + bridge)"
distros:
  - rhel10
  - almalinux10
doc_url: https://blog.stephane-robert.info/docs/admin-serveurs/linux/reseau/bond-bridge/
validation:
  functional: true
  security: false
  persistence_after_reboot: true
certification_tags:
  - lfcs
