id: l3-grub-kernel-args
title: "Add a persistent kernel boot parameter"
description: "Add a kernel command-line parameter to the current kernel with grubby and to /etc/default/grub for future kernels, so it survives reboots and updates — proven by grubby and the grub config."
section: l3
bloc_order: 2
lab_type: lab
level: l3
track:
  - exploiter
skills:
  - grub
  - grubby
  - kernel-parameters
  - bootloader
difficulty: intermediate
estimated_time: 20m
runtime:
  type: vm
  # On reste sur le poste : c'est à l'apprenant de taper son ssh vers
  # la machine du lab, et dsoxlab n'est installé que sur le poste.
  session: local
  default: rhel
  snapshot_required: false
  targets:
    - name: rhel
      host: alma-rhcsa-1.lab
      label_en: "AlmaLinux 10 (grubby, /etc/default/grub)"
      label_fr: "AlmaLinux 10 (grubby, /etc/default/grub)"
distros:
  - rhel10
  - almalinux10
doc_url: https://blog.stephane-robert.info/docs/securiser/durcissement/grub/
validation:
  functional: true
  security: false
  persistence_after_reboot: true
certification_tags:
  - rhcsa
