id: l3-scheduling-cron
title: "Schedule a recurring job with cron"
description: "Run /usr/local/bin/report.sh every day at 02:30 through a cron entry — proven by the actual schedule the cron daemon will honour."
section: l3
bloc_order: 6
lab_type: lab
level: l3
track:
  - exploiter
skills:
  - cron
  - crontab
  - scheduling
difficulty: intermediate
estimated_time: 20m
runtime:
  type: vm
  # On reste sur le poste : c'est à l'apprenant de taper son ssh vers
  # la machine du lab, et dsoxlab n'est installé que sur le poste.
  session: local
  default: rhel
  snapshot_required: false
  targets:
    - name: rhel
      host: alma-rhcsa-1.lab
      label_en: "AlmaLinux 10 (cron.d / crontab)"
      label_fr: "AlmaLinux 10 (cron.d / crontab)"
distros:
  - rhel10
  - almalinux10
  - ubuntu24.04
doc_url: https://blog.stephane-robert.info/docs/admin-serveurs/linux/exploiter/planification/cron/
validation:
  functional: true
  security: false
  persistence_after_reboot: true
certification_tags:
  - rhcsa
  - lfcs
